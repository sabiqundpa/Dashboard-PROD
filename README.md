# Dashboard-MTN

Maintenance monitoring dashboard — Express REST API, PostgreSQL
(via Prisma, hosted on Supabase), and a React (Vite) frontend with admin
login. Deployed on Vercel (static frontend + serverless function for
the API) with the database on Supabase.
